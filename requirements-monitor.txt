# Dipendenze del monitor (GitHub Actions ed esecuzione locale).
# Versioni con vincolo di maggiore: aggiornamenti di sicurezza sì,
# cambi di major che romperebbero i parser no.

requests>=2.31,<3

# Replica il fingerprint TLS/HTTP2 di un browser reale. È la contromisura
# anti-blocco che conta di più insieme all'indirizzo IP: senza, Vinted e
# Subito rispondono 403 molto più spesso. Se l'installazione fallisce su una
# piattaforma esotica, scrapers/http.py ripiega automaticamente su requests.
curl_cffi>=0.7,<1

beautifulsoup4>=4.12,<5
lxml>=5.1,<6

# YAML round-trip: preserva i commenti di config.yaml quando il bot Telegram
# lo modifica. PyYAML non è in grado di farlo.
ruamel.yaml>=0.18,<0.19
